version: 0.0
os: linux

files:
  - source: ./appspec.yml
    destination: /home/wyt
  - source: ./restartTomcat.sh
    destination: /home/wyt
  - source: ./build/libs/ROOT.war
    destination: /opt/tomcat/webapps

hooks:
  AfterInstall:
   - location: ./restartTomcat.sh
     timeout: 180
     runas: ubuntu
